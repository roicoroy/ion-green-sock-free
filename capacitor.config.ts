import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ion.green.sock',
  appName: 'ion-green-sock-free',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
